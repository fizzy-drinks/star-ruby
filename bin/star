#!/usr/bin/env ruby

require "json"
require_relative "../lib/app_builder"

app = Star::AppBuilder.build {
  instance_eval(File.read("./Starfile"), 'Starfile')
}

instance_exec(&app.method(:main)) if app.respond_to?(:main)
